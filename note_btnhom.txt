Khối “Import thư viện”: glob, PIL.Image, torch, torch.nn as nn, Dataset, DataLoader từ torch.utils.data, torchvision.transforms as T – các thư viện xử lý ảnh, tạo dataset/dataloader, biến đổi ảnh.

Comment cài timm nếu thiếu; import timm: dùng model EfficientNet pretrained.

device = torch.device('cuda' if torch.cuda.is_available() else 'cpu'); device: chọn GPU nếu có, in giá trị device khi notebook chạy.

Docstring “## 3. Dataset UTKFace”: mô tả cấu trúc tên file UTKFace (age_gender_race_timestamp.jpg) và mapping tuổi thành 5 nhóm.

from torch.utils.data import Dataset… (lặp import) và import os, torch: lại import để chắc chắn có trong scope.

Hàm age_to_group5(age: int) -> int: chia tuổi thành 5 nhóm (<=18→0, <=30→1, <=45→2, <=60→3, còn lại→4).

Lớp UTKFaceDataset(Dataset):

__init__(self, root, transform=None): lưu đường dẫn gốc, transform; khởi tạo samples rỗng.
Vòng lặp for fname in os.listdir(root): duyệt ảnh trong thư mục.
Bỏ qua file không có đuôi ảnh.
parts = fname.split("_"); bỏ qua nếu tên không đủ 4 phần.
Parse age, gender, race từ tên file; nếu lỗi thì continue.
age_group = age_to_group5(age): gộp tuổi vào 5 nhóm.
path = os.path.join(root, fname); self.samples.append((path, age_group, gender, race)): lưu thông tin.
In số ảnh hợp lệ tìm được.
__len__: trả về số mẫu.
__getitem__: mở ảnh, chuyển RGB, áp dụng transform; trả về dict chứa tensor ảnh và label age_group (age_cls), gender, race dưới dạng torch.tensor.
Khối “# 3.1. Transform và DataLoader”:

train_tfms: chuỗi biến đổi Resize 224×224, lật ngang ngẫu nhiên, ToTensor, Normalize theo ImageNet.
val_tfms: giống train nhưng không lật ngang.
Thiết lập train_dir, val_dir đường dẫn trên Drive.
Nếu thư mục tồn tại, tạo train_ds/val_ds với transform tương ứng và DataLoader (batch 32, shuffle cho train, num_workers=2); nếu không, gán None.
train_ds: hiển thị dataset (trên notebook sẽ in info).
Docstring “## 4. EfficientNet-B0 Multi-head”: mô tả sẽ dùng EfficientNet‑B0 (từ timm) làm feature extractor và tách 3 head cho age/gender/race.

Lớp EfficientNetMultiHead(nn.Module):

__init__: tạo backbone timm.create_model("efficientnet_b0", pretrained=True, num_classes=0, global_pool="avg") để lấy đặc trưng 1280-dim; tạo shared_fc (Linear 1280→512 + ReLU); 3 head tuyến tính cho age (5 lớp), gender (2 lớp), race (5 lớp).
forward: chạy backbone, qua shared_fc, xuất age_logits, gender_logits, race_logits.
model = EfficientNetMultiHead().to(device); model: khởi tạo và chuyển model lên device.

Docstring “## 5. Hàm train / eval 5 epoch”: mô tả cộng loss của 3 nhánh.

Hàm train_one_epoch(model, loader, optimizer, ce_age, ce_gender, ce_race) (phiên bản 1):

model.train(), đặt running_loss=0.
Lặp batch: lấy imgs/age/gender/race về device; zero_grad; forward; tính từng loss CrossEntropy; tổng loss; backward; optimizer.step; cộng dồn loss theo số mẫu. Trả về loss trung bình/epoch.
Decorator @torch.no_grad() và hàm eval_one_epoch: set eval mode; tính loss trung bình và accuracy cho từng nhánh:

Lặp batch, forward; tính loss_age/gender/race; tổng; cộng dồn loss theo số mẫu.
Lấy dự đoán argmax từng nhánh; đếm đúng; tính avg_loss = tổng loss / số mẫu; tính age_acc, gender_acc, race_acc; trả về.
Khối kiểm tra file tên lỗi:

bad_files = []; root = train_dir.
Lặp file, bỏ đuôi không phải ảnh; kiểm tra tách được age, gender, race; gom tên lỗi vào bad_files.
In số file lỗi và 10 file đầu.
Khởi tạo lại train_ds, val_ds với transform; tạo DataLoader train/val (batch 32…).
Định nghĩa lại train_one_epoch (phiên bản 2 có in debug):

Giống trên nhưng nhận thêm i (index batch) để in shape logits và max label ở batch đầu tiên nhằm debug nhãn.
Quy trình: forward → tính 3 loss → cộng → zero_grad/backward/step → cộng loss tích lũy → trả loss trung bình.
Đoạn debug DataLoader:

batch = next(iter(train_loader)): lấy một batch.
Lấy imgs/age/gender/race lên device; in shape.
with torch.no_grad(): forward model; in shape logits.
In max label và unique label (age, gender, race) để kiểm tra phạm vi nhãn.
Định nghĩa lại UTKFaceDataset khác (phiên bản kiểm tra file tồn tại):

Nhận root, transform; all_samples = build_samples_somehow(root) (placeholder user phải tự định nghĩa).
Lọc các sample có file tồn tại, in cảnh báo nếu thiếu; lưu self.samples; in tổng số ảnh valid.
__getitem__: mở ảnh, transform, trả dict (không convert label sang tensor ở đây).
__len__: trả số mẫu.
Hàm __getitem__ kế tiếp (viết rời) thêm logic nếu file mất thì chọn random sample khác, in cảnh báo; mở ảnh RGB, transform, trả dict. (Lưu ý: đây là snippet để thay vào class, không được gắn class trong code hiện tại).

Khối “# 6. Train 5 epoch”:

num_epochs = 5; định nghĩa 3 loss nn.CrossEntropyLoss().
optimizer = torch.optim.Adam(model.parameters(), lr=1e-4).
best_val_loss = inf; best_state = None.
Nếu cả train_loader và val_loader tồn tại: lặp epoch 1..5, gọi train_one_epoch, eval_one_epoch, in kết quả; lưu best_state nếu val_loss tốt hơn. Nếu không có data, in thông báo chưa tìm thấy.
“# 7. Lưu model tốt nhất”:

Nếu best_state có: tạo thư mục checkpoints, torch.save state_dict lên Drive path /content/drive/MyDrive/Nhom1_Py/efficientnet_multihead_best.pth, in đã lưu. Nếu không, in không có model để lưu.
Khối load và suy luận:

model = EfficientNetMultiHead().to(device): khởi tạo mới.
ckpt_path = "...best.pth"; state_dict = torch.load(..., map_location=device); model.load_state_dict(state_dict); model.eval(); in đã load.
img_tfms: transform cho suy luận (Resize 224, ToTensor, Normalize).
Import thêm torch.nn.functional as F, matplotlib.pyplot as plt.
Định nghĩa nhãn age_classes, gender_classes, race_classes.
Hàm predict_folder(folder_path, model, device, img_tfms):

Khởi results = []; lọc danh sách file ảnh hợp lệ trong thư mục (sorted).
Nếu không có file, in thông báo và return [].
In số ảnh tìm thấy, bắt đầu dự đoán.
Với mỗi file:
Đọc ảnh RGB, apply transform, thêm batch dim, to(device).
with torch.no_grad(): forward model.
Tính softmax từng nhánh để lấy xác suất; lấy argmax cho age/gender/race.
Hiển thị ảnh bằng plt.imshow, tắt trục, đặt title tên file.
In kết quả dự đoán và vector xác suất cho từng nhánh.
Append dict kết quả (đường dẫn, chỉ số nhãn, mảng xác suất) vào results.
In thông báo hoàn thành và trả results.
Cuối file: thiết lập folder_path mẫu và gọi predict_folder(folder_path, model, device, img_tfms) để chạy suy luận trên thư mục ảnh test trong Drive.