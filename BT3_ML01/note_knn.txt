Phần 1 – Dự đoán tiểu đường:

 Đọc dữ liệu từ diabetes_knn.csv vào DataFrame df.
: Tách đặc trưng X (tuổi, cân nặng, glucose, huyết áp) và nhãn y (có/không tiểu đường).
: Chia train/test 75%/25%, giữ tỷ lệ lớp bằng stratify=y, đặt seed 42.
: Tạo Pipeline gồm chuẩn hóa StandardScaler và KNeighborsClassifier với k=5, trọng số theo khoảng cách.
: Huấn luyện pipeline trên tập train.
: Dự đoán trên test, in accuracy, ma trận nhầm lẫn và báo cáo phân loại.
: Tạo một bệnh nhân mới (DataFrame một hàng) với các đặc trưng mẫu.
: Tính xác suất lớp 1 bằng predict_proba, và nhãn dự đoán bằng predict.
: In nhãn dự đoán (0/1) và xác suất tiểu đường làm tròn 3 chữ số.
: Khai báo lưới tham số cho KNN (số láng giềng, kiểu trọng số, chuẩn p=1/2).
: GridSearchCV 5-fold trên toàn bộ dữ liệu, tìm tham số tốt nhất và in điểm CV.
 – Nhận diện chữ số viết tay (Keras MNIST):

: Docstring mô tả bài toán MNIST và cách 1 (Keras).
: Import dataset MNIST từ Keras, các hàm cần thiết, và PIL để xử lý ảnh.
: Tải dữ liệu MNIST: 60k train, 10k test.
: Reshape ảnh 28x28 thành vector 784 chiều, ép kiểu float32 và chuẩn hóa về [0,1].
: Lấy subset ~10k mẫu để train nhanh, stratify theo nhãn, seed 42.
: Khởi tạo KNN k=3, trọng số theo khoảng cách, dùng đa luồng; huấn luyện trên subset.
: Dự đoán 2000 mẫu test đầu để nhanh, in accuracy và báo cáo phân loại.
: Lưu 5000 ảnh train đầu thành CSV phẳng 784 pixel + nhãn label, file mnist_5k_flat.csv.
Import matplotlib để lưu ảnh.
: Lấy ảnh test đầu tiên, reshape 28x28 và lưu ra digit.png màu xám.
: Hàm load_digit_image đọc ảnh, chuyển grayscale, resize 28x28, chuẩn hóa [0,1], trả về vector 1x784.
: Nạp digit.png, dự đoán nhãn và xác suất cao nhất, in kết quả.
 – MNIST qua OpenML:

: Docstring mô tả cách 2 (OpenML).
: Import fetch_openml, KNN, train_test_split, accuracy_score, pandas.
: Tải MNIST_784 từ OpenML, chuẩn hóa pixel về [0,1], nhãn thành int.
: Chia train 12k, test 2k, stratify, seed 42.
: Khởi tạo KNN k=3, trọng số khoảng cách, đa luồng; huấn luyện.
: Dự đoán trên test, in accuracy.
: Lưu tập train con thành mnist_subset_train.csv (784 cột pixel + nhãn).
 – Digits 8x8 (sklearn load_digits):

: Docstring mô tả cách 3 (bộ digits 8x8 nhỏ).
: Import load_digits, split, KNN, accuracy_score.
: Tải bộ digits 8x8 (1797 ảnh), chuẩn hóa pixel chia 16, lấy nhãn.
: Chia train/test 75%/25%, stratify, seed 42.
: Huấn luyện KNN k=3, trọng số khoảng cách trên bộ digits, in accuracy trên test