Phần “siêu thị”:

df = pd.read_csv("supermarket_customers_kmeans.csv") → Đọc dữ liệu khách hàng.
X = df[...] .copy() → Lấy 4 cột đặc trưng làm dữ liệu huấn luyện.
scaler = StandardScaler() → Khởi tạo bộ chuẩn hóa.
X_scaled = scaler.fit_transform(X) → Tính tham số chuẩn hóa và áp dụng cho X.
inertias, sils, Ks = [], [], range(2, 9) → Chuẩn bị danh sách lưu inertia, silhouette, và dải K thử (2–8).
for k in Ks: → Lặp qua các giá trị K.
km = KMeans(n_clusters=k, n_init=10, random_state=42) → Tạo model K‑means với k cụm, 10 lần khởi tạo, cố định seed.
km.fit(X_scaled) → Huấn luyện trên dữ liệu chuẩn hóa.
inertias.append(km.inertia_) → Lưu tổng bình phương khoảng cách trong cụm (elbow).
sils.append(silhouette_score(X_scaled, km.labels_)) → Lưu điểm silhouette cho nhãn tìm được.
plt.figure(); plt.plot(...); ...; plt.show() (2 khối) → Vẽ biểu đồ Elbow và Silhouette theo K.
k = 4 → Chọn 4 cụm (theo mô tả).
kmeans = KMeans(...); labels = kmeans.fit_predict(X_scaled) → Tạo/huấn luyện K‑means với k=4 và lấy nhãn cụm.
df["Cluster"] = labels → Gán nhãn cụm vào bảng gốc.
centers_scaled = kmeans.cluster_centers_ → Lấy tâm cụm ở không gian chuẩn hóa.
centers = pd.DataFrame(scaler.inverse_transform(centers_scaled), columns=X.columns) → Đưa tâm cụm về thang đo gốc, tạo DataFrame với tên cột gốc.
centers["Cluster"] = range(k) → Thêm cột chỉ số cụm.
print("Cụm...") → In tâm cụm đã làm tròn.
df.to_csv("supermarket_customers_kmeans_labeled.csv", index=False) → Lưu dữ liệu kèm nhãn cụm.
print("Đã lưu...") → Thông báo lưu file.
Dự đoán khách hàng mới:

new_cust = pd.DataFrame([{...}]) → Tạo DataFrame 1 khách hàng mới.
new_scaled = scaler.transform(new_cust) → Chuẩn hóa theo scaler đã fit.
cluster_id = kmeans.predict(new_scaled)[0] → Dự đoán cụm.
print("Khách hàng mới...") → In cụm dự đoán.
from numpy.linalg import norm → Hàm chuẩn Euclid.
dists = [norm(new_scaled - c) for c in kmeans.cluster_centers_] → Khoảng cách tới từng tâm cụm.
print("Khoảng cách...") → In khoảng cách làm tròn.
ax_x, ax_y = ... → Chọn hai trục để vẽ 2D.
Vòng for c in sorted(df["Cluster"].unique()): → Vẽ scatter từng cụm trên hai trục đã chọn.
plt.xlabel...; plt.legend(); ...; plt.show() → Thiết lập nhãn, tiêu đề, hiển thị.
Phần “nhận diện khuôn mặt”:

Docstring giải thích bối cảnh gom cụm khuôn mặt.
df = pd.read_csv("face_embeddings_synthetic.csv") → Đọc embeddings khuôn mặt.
X = df.filter(like="f").to_numpy() → Lấy các cột đặc trưng f000..f127 thành mảng NumPy.
Comment tùy chọn bỏ nhãn mô phỏng.
scaler = StandardScaler(); X_scaled = scaler.fit_transform(X) → Chuẩn hóa embeddings.
Ks = range(2, 10); inertias, sils = [], [] → Thiết lập dải K và mảng lưu chỉ số.
Vòng for k in Ks: → Thử từng K:
km = KMeans(...); labels = km.fit_predict(X_scaled) → Huấn luyện và lấy nhãn.
inertias.append(...); sils.append(...) → Lưu inertia và silhouette.
Hai khối plt.figure(); plt.plot(...); ... → Vẽ Elbow và Silhouette.
k = 5 → Chọn 5 cụm (giả lập 5 người).
kmeans = KMeans(...); labels = kmeans.fit_predict(X_scaled) → Huấn luyện cuối cùng và lấy nhãn.
df["cluster_id"] = labels → Lưu nhãn cụm vào DataFrame.
print(df[["image_name", "cluster_id"]].head(10)) → Xem nhanh 10 dòng đầu với tên ảnh và cụm.
if "person_simulated" in df.columns: ... → Nếu có nhãn mô phỏng, in bảng chéo nhãn vs cụm.
cluster_to_name = {...} → Bản đồ gán tên người cho từng cụm (thủ công).
df["identity_assigned"] = df["cluster_id"].map(cluster_to_name) → Thêm cột tên người.
df.to_csv("face_embeddings_clustered.csv", index=False) → Lưu kết quả.
print("Đã lưu...") → Thông báo lưu file.
Dự đoán ảnh mới:

x_new = np.random.normal(0, 1, size=(1, X.shape[1])) → Tạo ngẫu nhiên 1 embedding mới (placeholder).
x_new_scaled = scaler.transform(x_new) → Chuẩn hóa embedding mới.
cid = kmeans.predict(x_new_scaled)[0] → Dự đoán cụm.
print("Ảnh mới thuộc cụm:", ...) → In cụm dự đoán.
print("=> Gán người:", ...) → In tên người nếu đã gán.
Giảm chiều vẽ 2D:

from sklearn.decomposition import PCA → Nạp PCA.
pca = PCA(n_components=2, random_state=42) → Khởi tạo PCA 2 chiều.
pts2d = pca.fit_transform(X_scaled) → Giảm chiều embeddings xuống 2D.
Vòng for c in sorted(np.unique(labels)): → Vẽ scatter các điểm 2D theo cụm.
plt.legend(); plt.title(...); plt.show() → Hiển thị biểu đồ cụm trong không gian 2D.